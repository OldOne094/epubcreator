<div dir="rtl">

# خطة تحسين وتطوير EPubCreator — متكاملة ومتناسقة

> نسخة: 1.0 — وثيقة عمل (Roadmap) تغطّي ثلاث ركائز مترابطة:
> **(أ) خفض حجم الحزمة** · **(ب) تطوير الميزات وتصحيح الثغرات** · **(ج) تحسين تجربة المستخدم**،
> مع محور رابع **(د) للجودة والهندسة** يضمن تماسك الخطوات مع بعضها.
>
> خط الأساس: **154 اختبارًا ناجحًا** · الحزمة `dist\EPubCreator.exe` = **57MB** · واجهة عربية RTL (PySide6 6.11).

---

## سجل التنفيذ (Changelog)

| التاريخ | البند | الحالة |
|---|---|---|
| Sprint 0 | **B8** كسر التبعية المعكوسة (`epub.py ← ui/preview`) — صار `core/epub.py` يستورد `split_paragraphs` من `core/format.py` | ✅ منفّذ |
| Sprint 0 | **B1/B2** إصلاح صيغ الغلاف: الامتداد/البايتات/MIME تطابق PNG/WebP/JPEG في المولّد والغلاف | ✅ منفّذ + اختبارات |
| Sprint 0 | **B5** تأكيد قبل حذف فصل | ✅ منفّذ |
| Sprint 0 | **B4** معاينة الغلاف حيّة عند تغيير العنوان/القالب (بكاش بصمة) | ✅ منفّذ |
| Sprint 0 | **B7** تقدم تصدير فعلي: `EpubWriter(on_progress)` → `ExportJob.progress` → شريط تقدم في صفحة التصدير | ✅ منفّذ + اختبار |
| Sprint 0 | **B6** تراجع/إعادة هيكلية في `BookState` (لقطة عميقة للكتاب، سقف 50) + زرا تراجع/إعادة في المحرر | ✅ منفّذ + اختبارات |
| Sprint 1 | **C1** حفظ/فتح المشروع `.epubproj` (JSON) + اختصارات `Ctrl+S/Shift+S/Shift+O` + قائمة "مشاريع حديثة" (آخر 8) + عنوان نافذة يعكس حالة الحفظ (•) + تأكيد عند الإغلاق | ✅ منفّذ + اختبارات |
| Sprint 1 | **B3/C3** تضمين الخطوط فعليًّا: `@font-face` + نسخ إلى `OEBPS/fonts/` + manifest/media-type صحيحة + `package_fonts_dir` | ✅ منفّذ + اختبارات (5) |
| Sprint 1 | **تصدير بلقطة آمنة** — `ExportJob` يلتقط الكتاب لحظة الإنشاء (`deepcopy`) فلا تتأثر الكتابة بتحرير المستخدم أثناء البناء | ✅ منفّذ + اختبار |
| Sprint 1 | **استيراد دفعة** — تحميل أول كتاب مع إشعار في شريط الحالة بعدد الكتب المستوردة | ✅ منفّذ + اختبار |
| Sprint 2 | **توحيد الإعدادات** — `theme`/`recent_projects`/`geometry` في `DEFAULT_CONFIG` + مساعدات `get_config/set_config` + تذكّر هندسة النافذة | ✅ منفّذ + اختبارات |
| Sprint 2 | **نظافة** — إزالة `EPubCreatorOne.spec` وحزمة `build/` القديمة؛ بقي `epubcreator.spec` وحيدًا | ✅ |
| Sprint 2 | **نظام تحديثات** — `app/updates.py` (مقارنة إصدارات + جلب JSON من `config: update_url`) + `UpdateCheckJob` في الخلفية + قائمة "تحقق من التحديثات…" + مركزة الإصدار في `app/__init__.py` + إقلاع سريع للحزمة عبر `EPUBCREATOR_SMOKE` | ✅ منفّذ + اختبارات (12) |
| Sprint 2 | **ربط GitHub Releases API** — `update_url` يقبل `owner/repo` (أو رابط API كامل) ويفسّر `tag_name`/`html_url`/`assets`/`body` وأخطاء 404/rate-limit من GitHub | ✅ منفّذ + اختبارات (7) |
| Sprint 2 | **إصلاح تبديل الثيم** — كان الإقلاع يطبّق النمط على النافذة فيظلّ الفاتح يطغى؛ أصبح الثيم يُطبَّق على مستوى QApplication دائمًا | ✅ منفّذ + اختبارات |
| Sprint 3 | **الشعار + v0.3.0** — لوغو (كتاب مفتوح على لوحة ذهبية) مولّد بـ `tools/make_logo.py` → `logo.png` + `EPubCreator.ico` (7 مقاييس)؛ أيقونة EXE + نافذة + حوار "حول" + الشريط الجانبي؛ وترقية الإصدار إلى **0.3.0** | ✅ منفّذ + اختبارات |

> الاختبارات: **139 → 154** (15 اختبارًا جديدًا لصيغ الغلاف، التراجع، والتقدم)·· **179** بعد سبرنت 1/2 ·· **191** بعد نظام التحديثات·· **198** بعد دعم GitHub·· **202** بعد الشعار والإصدار 0.3.0.

> **البناء**: أُعيد بناء `dist\EPubCreator.exe` (56.7MB) عبر `pyinstaller epubcreator.spec` — الآن بأيقونة مدمجة — واجتاز إقلاعًـا فعليًّـا (`QT_QPA_PLATFORM=offscreen` + `EPUBCREATOR_SMOKE=1` → exit 0).

---

## 1. الوضع الحالي (تشخيص موجز)

| البند | الواقع | المرجع |
|---|---|---|
| الحزمة | onefile عبر PyInstaller، ~57MB | `epubcreator.spec` |
| الميزات | استيراد TXT/MD/HTML/DOCX/RTF، محرر فصول، بيانات وصفية، 5 قوالب، غلاف، معاينة، تصدير EPUB2/3، تحقق | `README.md` |
| الحالة | كل شيء في الذاكرة — **لا حفظ/فتح مشروع** | `state.py` |
| ميزات وهمية | `embed_fonts=True` بلا تنفيذ؛ خيار PNG للغلاف لا يعمل | `models.py:64` · `epub.py:157` · `covergen.py:160` |
| تبعية معكوسة | ~~`core/epub.py` يستورد من `ui/preview.py`~~ — أُصلحت | ✅ |
| ثبات | 154 اختبارًا يمرّ (~19s) | `tests/` |

---

## 2. الأهداف المقيسة (مؤشرات النجاح)

| المؤشر | الحالي | المستهدف |
|---|---|---|
| حجم الحزمة EXE | 57MB | **≤35MB** (و~28MB مع UPX) |
| زمن الإقلاع | ~2–3s | **<2s** |
| حفظ/فتح المشروع | غير موجود | مدعوم + قائمة ملفات حديثة |
| نصوص داخل الفصول (صور) | لا تُستورد | مدعومة |
| تضمين الخطوط | خيار وهمي | يعمل فعليًّا |
| الاختبارات | 154 | تتوسع مع كل ميزة وتبقى خضراء |
| إتاحة/ثيم | ثابت فاتح/داكن | تتبّع النظام + تباين AA |

> القاعدة الحاكمة: **كل خطوة تقيس أثرها** — لا تغيير هندسي بلا اختبار، ولا ميزة بلا تأكيد أنها تُسقِط حجمًا أو ترفع سهولة الاستخدام.

---

## 3. المحور (أ) — خفض الحجم

### 3.1 ميزانية الحجم الحالية (تقديرية)

| المساهم | الحجم التقريبي | يمكن خفضه؟ |
|---|---|---|
| PySide6 (QtCore/QtGui/QtWidgets + plugins) | ~38MB | نعم (Essentials + UPX) |
| `lxml` + `python-docx` | ~10MB | نعم (إسقاط لصالح stdlib) |
| Pillow + الترميزات | ~5MB | نعم (استبدال بـ Qt/QImage) |
| Python runtime + DLLs | ~6MB | جزئيًا (strip) |
| arabic_reshaper + python-bidi + markdown-it + striprtf + platformdirs | ~2MB | جزئيًا |
| app + assets | <0.5MB | — |

### 3.2 خطوات الخفض مرتبة بالأثر

**① تفعيل UPX (فوري، أثر 30–40%)**
- `upx=True` في `epubcreator.spec:65` مع جعل UPX متاحًا في `PATH`.
- مخاطرة: بعض مضادات الفيروسات تُبلّغ عن ملفات UPX المضغوطة كإيجابيات كاذبة.
- **قرار**: جرّبه أولًا وقِس؛ إن ظهرت بلاغات AV تُراجع أو تُبقى `False` (الخطوات أدناه تغطّي المكسب بدونه).
- متوقع: **57 → ~38MB**.

**② الانتقال إلى `PySide6-Essentials` بدل `PySide6` الكامل**
- الحزمة الكاملة تسحب وحدات لا نستخدمها (QtNetwork/QtSql/QtXml/QtSvg/QtOpenGL/Qt3D/…). الاستبدال في `pyproject.toml` و`requirements.txt` مع حذف مدخلات الـ excludes المكررة في المخطط.
- التطبيق يستخدم فقط `QtCore + QtGui + QtWidgets` — كلها ضمن Essentials.
- متوقع: خفض إضافي ~3–5MB (حِزم + plugins).

**③ إسقاط `lxml` و `python-docx` (الأكبر بعد Qt)**
- `lxml` مستخدم فقط في: `importers.py:10` (تحليل HTML) و `validate.py:8` (تحليل XML) وضمن `python-docx`.
- المسار: استبدال تحليل XML بـ `xml.etree.ElementTree` (stdlib) في `validate.py`، و`html.parser` لتنظيف HTML في `importers.py`، وقراءة DOCX مباشرة بـ `zipfile + ElementTree` (docx = ZIP من XML: `word/document.xml` + أنماط العناوين) — نموذج `docx_to_chapters` الموجود يُعيد كتابته بلا اعتماد.
- المكسب: **~10MB** وإزالة تبعيتين كاملتين.
- **قرار معلّق**: إن كان زمن التنفيذ قصيرًا، البديل الجزئي — إبقاء `python-docx` وإخراج `lxml` من `validate.py` فقط — يحفظ ~8MB. الأولوية للمسار الكامل ليعيد الاستثمار في صيانة الكود.

**④ إسقاط Pillow لصالح Qt (يرتبط بتطوير الغلاف في المحور ب)**
- الغلاف يُولَّد حاليًّا عبر Pillow + reshaper + bidi (`covergen.py`). بإعادة كتابته على `QImage + QPainter`:
  - رسم العنوان العربي **بتشكيل تلقائي صحيح** عبر QFont/Qt (HarfBuzz) — بلا reshaper/bidi.
  - قراءة/ضغط/تحويل الصور عبر `QImageReader/QImageWriter` (jpeg/png/webp) — يعمل داخل خيط التصدير (QImage آمنة خارج خيط الواجهة، بخلاف QPixmap).
- المكسب: **~6MB** وإسقاط ثلاث تبعيات (`Pillow`, `arabic_reshaper`, `python-bidi`).

**⑤ خفضات إضافية (نظافة)**
- `strip=True` للملفات الثنائية (قد يؤثر على بعض الامتدادات؛ يُختبر).
- حذف مدخلات excludes المكررة بعد Essentials + إضافة ما تبقى (`QtTest`, `QtConcurrent`, `QtDBus`, `QtUiTools`, `QtHelp`, `QtSql`).
- لا تُضمَّن ترجمات Qt (`qt_*.qm`) — تقليل مضمن أصلًا؛ يُتأكد منه.

### 3.3 النتيجة المتوقعة

| السيناريو | الحجم |
|---|---|
| الحالي | 57MB |
| ① UPX فقط | ~38MB |
| ①+② (UPX + Essentials) | ~33MB |
| ①②③④ كاملة | **~28MB** (و~25MB مع strip) |

> **ترابط**: الخطوة ③ تُبسّط الاختبارات (لا lxml)، والخطوة ④ تُمكّن إصلاح غلاف PNG/WebP، و② ترفع استقرار البناء. لا تُنفَّذ ④ قبل مرور ① لأن قياس كل أثر يلزم أن يكون مستقلاً.

---

## 4. المحور (ب) — تطوير الميزات وتصحيح الثغرات

### 4.1 إصلاحات عاجلة (تُنفَّذ أولًا؛ بعضها يلامس تجربة المستخدم)

| # | المشكلة | الموقع | الحل |
|---|---|---|---|
| B1 | اختيار **PNG** للغلاف ينتج ملف `cover.jpeg` ببايتات PNG ونوع MIME jpeg → غلاف تالف | `epub.py:157` (`cover_href = "cover.jpeg"` دائمًا) · `covergen.py:160` | استنتاج الامتداد/MIME من `image_format` فعليًّا، وتسمية الملف المطابق، وتحديث OPF |
| B2 | اختيار **webp** يُكتب فعليًّا كـ JPEG | `covergen.py:160` (`fmt = "JPEG" if != "png" else "PNG"`) | استخراج الصيغة من الخيار مباشرة؛ وضبط `_IMAGE_MEDIA` |
| B3 | `embed_fonts=True` **بلا تنفيذ** + مجلد الخطوط فارغ | `models.py:64` · `assets/fonts/` | **تنفيذها** (انظر C3 أدناه) أو إزالة الخيار — التوصية: تنفيذها لأنها قيمة عربية حقيقية |
| B4 | معاينة الغلاف لا تتحدث تلقائيًّا عند تغيير العنوان | `cover_page.py:165` (زر يدوي) | ربط تغيّر `metadata.title` بتحديث المعاينة عبر `state.notify()` |
| B5 | حذف فصل بلا تأكيد | `pages.py:128` | QMessageBox تأكيد قبل الحذف |
| B6 | لا تراجع/إعادة (Undo/Redo) في المحرر | `pages.py` | `QUndoStack` أو أخذ لقطة حصرية لعمليات الفصل |
| B7 | تصدير بلا نسبة تقدم فعلية | `workers.py:59` (`ExportJob` بلا progress) | `Signal(int)` للمراحل (تجميع → ضغط → غلاف) + عرض % في صفحة التصدير |
| B8 | التبعية المعكوسة `core/epub.py ← ui/preview.py` | `epub.py:12` | نقل `split_paragraphs` إلى `core/format.py` وإعادة التصدير من `ui/preview.py` للمحافظة على الواجهات |

### 4.2 ميزات تطويرية (مرتبة بالأولوية)

**C1. حفظ/فتح المشروع + قائمة ملفات حديثة + حفظ تلقائي** (الأعلى قيمة)
- تنسيق `.epubproj` (JSON): الفصول + البيانات الوصفية + خيارات التنسيق/الغلاف.
- اختصارات `Ctrl+S` / `Ctrl+O`، وإدارة "آخر 8 مشاريع"، وحفظ تلقائي دوري في مجلد مؤقت مع استرجاع عند الإقلاع بعد انقطاع.

**C2. صور داخل الفصول**
- استيراد الصور من HTML (`<img>`) وDOCX (فك الضغط إلى مجلد مؤقت) مع ربط مسارات نسبية.
- إدراج صورة من المحرر + ضغط تلقائي عند التصدير (`max_image_width/compress` قائمان) — يفعّل إعدادات `cover_page` نفسها للمحتوى.

**C3. تضمين الخطوط فعليًّا (إحياء embed_fonts)**
- نسخ TTF (Amiri / Traditional Arabic — من خطوط Windows) إلى `OEBPS/fonts/` + قواعد `@font-face` في CSS المولّد.
- يرفع جودة العرض العربي في القرّاء (هذه هي القيمة التي بُنيت القوالب حولها)، ويرتبط مباشرة بـ B3.

**C4. ترقية فهرس EPUB3**
- شجرة TOC متعددة المستويات (h1→h2→h3) بدل الفهرسة المسطحة، وإضافة `landmarks` و `page-list` الاختيارية، وتقرير OPF أدق للغلاف (spine + guide).

**C5. بحث واستبدال + إحصاءات**
- `Ctrl+F` في الفصل/الكتاب، وإحصاء كلمات/أحرف/فقرات لكل فصل وللكتاب.

**C6. تحسينات المحرر**
- إعادة ترتيب الفصول بالسحب والإفلات (يرتبط بمسار `BookState.move_chapter` الموجود)، ودمج/تقسيم فصل، وتسمية تلقائية للفصول الجديدة.

**C7. قوالب + تخصيص**
- قوالب إضافية (مخطوطة، رقمية، سير ذاتي) وقرص ألوان قابل للتعديل يُنشئ قالبًا مخصصًا (يرتبط بـ `templates.py` و`covergen._template_colors`).

**C8. إعدادات افتراضية + تصدير دفعة**
- نافذة إعدادات توحّد الافتراضيات (قالب، خطوط، فقرة، غلاف، EPUB3/2) وتكتب في `config.json` عبر `app/settings.py`.
- تصدير كل الكتب المستوردة دفعة واحدة (يرتبط بـ `ImportJob` الحالي الذي يستورد كل ملف ككتاب مستقل).

### 4.3 ترابط ميزات هذا المحور

```
حفظ/فتح (C1) ──► يخدّم كل صفحة (محرر/بيانات/تنسيق)
    │
    ├── يطبّق على الصور (C2) والخطوط (C3) عبر المسارات النسبية
    └── يسهّل تصدير الدفعة (C8)
C4 (فهرس) ←── يعتمد على مستوى العناوين في المستوردين (موجود)
B4+B7      ←── يرفعان جودة المعاينة والتقدّم ويعتمدان على notify() الحالي
```

---

## 5. المحور (ج) — تحسين تجربة المستخدم

### 5.1 خريطة الرحلة الحالية → المستهدفة

| النقطة | الحالي | المستهدف |
|---|---|---|
| الإقلاع | استيراد كسول جزئي | إقلاع <2s مع تأخير تحميل كل صفحة عند فتحها |
| أول استخدام | صفحة رئيسية ساكنة | إرشاد أول (أول 3 خطوات) + بطاقة "مشروع سريع" |
| الحالة الفارغة | نص إرشادي فقط | مكوّنات رسومية واضحة لكل صفحة |
| الملاحظات | حوارات QMessageBox | إشعارات toast غير حاجزة للأحداث غير الحرجة + حوارات فقط للحرجة |
| الإجهاض | لا تأكيد | تأكيد للأعمال الهدامة (حذف فصل/مسح مشروع) |
| الاختصارات | Ctrl+O / Ctrl+E فقط | Ctrl+S/N/Z/Y/F، Enter لتأكيد النماذج |
| الإتاحة | أساسية | تباين AA، حلقات تركيز مرئية، عناوين tooltip لكل الحقول |

### 5.2 تحسينات واجهة مباشرة (تلمس الكود الحالي)

1. **ثيم يتبع النظام**: زر الثيم الحالي (`sidebar.py:80`) يتحول إلى ثلاثي "فاتح/داكن/تلقائي" عبر خاصية `Qt.ApplicationState` أو `QStyleHints.colorScheme`.
2. **تذكّر النافذة**: حفظ/استعادة حجم وموقع النافذة في `config.json`.
3. **أيقونة تطبيق + رقم إصدار موحّد**: `v0.1.0` محرف يدويًّا في `sidebar.py:85`؛ يُمركز في `app/__init__.py` ويُقرأ من `pyproject.toml`، مع أيقونة `.ico` في الحزمة (`datas`).
4. **معاينة الغلاف فورية**: ربط التغييرات مباشرة بـ `B4` — المستخدم يرى الغلاف يتحدث أثناء الكتابة.
5. **تحسين شريط الحالة** (`main_window.py:68`): عرض كلمات/فصول الكتاب + آخر عملية + نسبة تصدير في سطر واحد.
6. **حقول فارغة صامتة**: رسائل مساعدة داخل الحقول (placeholder) موجودة جزئيًّا — تُكمل في كل الحقول.
7. **إتاحة RTL**: التأكد من `dir` و`setLayoutDirection` في الحوارات المخصصة (`dialogs.py`) أيضًا، لا النافذة فقط.

### 5.3 أولوية إدراك القيمة (فارق يحسّه المستخدم أولًا)

1. الحفظ/الفتح (الخوف من فقدان العمل) — C1
2. زمن الإقلاع والحجم (أول انطباع) — المحور أ
3. الخطوط والغلاف السليم (جودة الكتاب الناتج) — B1/B2/C3
4. التقدم والمؤشرات الواضحة — B7
5. الاختصارات والتدفق — C5/C6

---

## 6. المحور (د) — الجودة والهندسة (يمسك بتماسك الخطة)

- **كسر التبعية المعكوسة** (`epub.py ← preview.py`) → بنية نظيفة: `core` لا يستورد من `ui` أبدًا (B8).
- **توحيد الإعدادات**: كل المفاتيح في `DEFAULT_CONFIG` (`settings.py:22`) مع إضافة new keys (theme، geometry، recent files، defaults) وتفريغ التخزين المبعثر في `main_window.py`.
- **مركزة الإصدار** والمسارات في موضع واحد.
- **i18n (اختياري متأخر)**: نقل نصوص الواجهة العربية إلى ملف موارد — المرحلة الأخيرة لأنها لا تغيّر السلوك.
- **الاختبارات**: لكل إصلاح/ميزة اختبار جديد (الخط الحالي 139). مطلوب على الأقل: غلاف PNG/webp (B1/B2)، حفظ/فتح (C1)، تضمين خطوط (C3)، TOC شجري (C4)، Undo (B6).
- **CI + بناء مُتحقق**: سكربت يبني EXE ثم يشغّله بـ `QT_QPA_PLATFORM=offscreen` ويستدعي اختبارات إقلاع سريع.
- **نظافة المستودع**: إضافة `build/`, `dist/`, `__pycache__/`, `.pytest_cache/` إلى `.gitignore` وتفعيل git (حاليًا بلا مستودع).

---

## 7. خارطة الطريق الزمنية والترابط (Sprints)

| المرحلة | المحتوى | يعتمد على | المخرَج القابل للقياس |
|---|---|---|---|
| **Sprint 0 — تثبيت وحماية** | B1–B8 (الإصلاحات) + ① UPX | خط الأساس الأخضر (139) | EXE أصغر + بلا أخطاء غلاف |
| **Sprint 1 — بقاء العمل** | C1 (حفظ/فتح/تلقائي) + undo + تأكيدات + B7 تقدم | Sprint 0 | حفظ/استعادة + تقدم حقيقي |
| **Sprint 2 — جودة الكتاب** | C3 خطوط + C2 صور + C4 فهرس | C1 (المسارات) | EPUB يحتوي خطوطًا وصورًا وفهرسًا شجريًّا |
| **Sprint 3 — الإنتاجية** | C5 بحث/إحصاء + C6 محرر + C7 قوالب + C8 دفعة/إعدادات | Sprint 1 | تدفق عمل أسرع للمستخدم |
| **Sprint 4 — الحجم النهائي والهندسة** | ② Essentials + ③ إسقاط lxml/docx + ④ Pillow→Qt + ⑤ strip + i18n + CI | Sprint 0–3 (كل قياس على أثر مستقل) | EXE ≤28MB + مستودع نظيف + CI خضراء |

> **التكامل**: لا يتقدم Sprint 4 على Sprint 0؛ لأن كل قياس خفض الحجم يتطلب أساسًا ثابتًا. وميزات Sprint 2 (خطوط/صور) تلمس نفس الكود الذي يلامسه خفض الحجم في Sprint 4 — لذلك تُنفَّذ الميزات أولًا ثم يُقاس الحجم بعد ثباتها، لا قبلها.

---

## 8. قائمة القرارات المفتوحة (تحتاج تأكيدًا)

| القرار | الخيارات | الأثر |
|---|---|---|
| UPX؟ | فعّله أولًا ثم تراجع إن ظهرت بلاغات AV | -19MB فورًا |
| المسار الكامل لإسقاط lxml (إعادة كتابة محلل DOCX) أم الجزئي (إبقاء python-docx)؟ | كامل: -10MB · جزئي: -8MB | جهد 2–4 أيام مقابل 1–2 |
| إسقاط Pillow لصالح QPainter للغلاف؟ | نعم: -6MB وثلاث تبعيات · لا: يبقى مستقر | يجب اختبار جودة تشكيل العنوان في القرّاء |
| i18n متعدد اللغات الآن أم لاحقًا؟ | لاحقًا (Sprint 4) | بلا أثر سلبي على الحجم |

---

## 9. الخلاصة التنفيذية

1. **الأولوية القصوى**: إصلاحات الغلاف (B1/B2) وخاصية الحفظ/الفتح (C1) — أكبر أثر سلبي/إيجابي يلمسه المستخدم.
2. **خفض الحجم**: ابدأ بـ UPX (فوري)، ثم Essentials، ثم إسقاط lxml/Pillow — الوصول إلى ~28MB متاح دون المساس بالميزات.
3. **تجربة المستخدم**: التركيز على زمن الإقلاع، التقدم الواضح، الثيم التلقائي، الإتاحة، وتذكّر تفضيلات المستخدم.
4. **التماسك**: كل مرحلة تقيس أثرها وتضيف اختباراتها، وتبقى البنية النظيفة (بلا تبعيات معكوسة) شرطًا قبل أي توسع.

</div>
