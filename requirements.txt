# مصدر الحقيقة للتبعيات: pyproject.toml (هذه القائمة مرآة مثبتة للتثبيت المباشر).
# أي تحديث إصدار يُجرى هناك أولًا ثم يُعكس هنا.
# Runtime (مطابق لـ [project].dependencies)
PySide6==6.11.1
lxml==6.1.1
python-docx==1.2.0
striprtf==0.0.32
markdown-it-py==4.2.0
Pillow==12.3.0
platformdirs==4.11.0
arabic_reshaper==3.0.1
python-bidi==0.6.11

# Dev (مطابق لـ [project.optional-dependencies].dev)
pytest

# Build
pyinstaller==6.21.0
